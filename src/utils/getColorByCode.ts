import { getColours } from "./api"

export const getColorByCode = (code: number) => {
    // if (!colorCache) {
    //     getColours().then(colors => {
    //         colors.forEach(({ name, code }) => {
    //             colorCache[code] = name
    //         });
    //     })
    // }

    // return colorCache[code]

    return code

}